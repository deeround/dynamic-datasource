<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.github.deeround.dynamic.datasource.samples.mapper.UserMapper">
    <insert id="insert1" parameterType="com.github.deeround.dynamic.datasource.samples.entity.TestUser">
        insert into test_user(id, name)
        values (#{id}, #{name})
    </insert>
    <insert id="insert2" parameterType="com.github.deeround.dynamic.datasource.samples.entity.TestUser">
        insert into test_user(id, name)
        values (#{id}, #{name})
    </insert>
</mapper>
